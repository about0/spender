import { combineReducers } from 'redux';
import from 'actions'

const initialState = {
  transactions: [],

};

function transaction(state = initialState, action) {

  switch(action.type) {
    case
  }
}
